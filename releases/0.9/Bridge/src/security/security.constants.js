const SECURITY_DEFAULTS = Object.freeze({
  MAX_JSON_BODY_SIZE: '1mb',
  CLIENT_PORTAL_RATE_LIMIT_WINDOW_MS: 15 * 60 * 1000,
  CLIENT_PORTAL_RATE_LIMIT_MAX: 100,
  AUTH_RATE_LIMIT_MAX: 10,
  ALLOWED_METHODS: ['GET', 'POST', 'PATCH', 'PUT', 'DELETE', 'OPTIONS'],
});

const REQUIRED_ENV_VARS = Object.freeze([
  'DATABASE_URL',
  'CLIENT_PORTAL_JWT_SECRET',
]);

module.exports = {
  SECURITY_DEFAULTS,
  REQUIRED_ENV_VARS,
};
