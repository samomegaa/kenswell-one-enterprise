# Payroll Processing Orchestrator & State Machine

The state machine validates sequential transitions. The orchestrator owns
execution metadata and exposes optional Enterprise event and audit adapter
callbacks. Staffology continues to own payroll calculations.
