'use strict';

module.exports = Object.freeze({
  workingPattern: Object.freeze([
    'workingPattern',
    'workPattern',
    'workingPatternName',
  ]),

  normalHours: Object.freeze([
    'normalHours',
    'standardHours',
    'hoursPerWeek',
    'usualHours',
  ]),

  normalDays: Object.freeze([
    'normalDays',
    'daysPerWeek',
    'usualDays',
  ]),

  payrollFrequency: Object.freeze([
    'payrollFrequency',
    'payRunFrequency',
    'frequency',
  ]),
});
