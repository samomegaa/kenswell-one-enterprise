const ConfigurationSource = Object.freeze({
  DEFAULT: 'default',
  ENVIRONMENT: 'environment',
  FILE: 'file',
  MEMORY: 'memory',
  REMOTE: 'remote',
  TENANT: 'tenant',
});

module.exports = {
  ConfigurationSource,
};
