import PropTypes from 'prop-types';
export default function PayrollRunStatusPanel({model}) { const current=model.current; return <section className="payroll-run-panel"><div className="payroll-run-panel-heading"><div><p className="payroll-run-eyebrow">Current payroll period</p><h2>{current?.name || 'Unavailable'}</h2></div><span className="payroll-run-read-only">Read only</span></div><dl className="payroll-run-status-grid"><div><dt>Processing status</dt><dd>{current?.statusLabel || 'Unavailable'}</dd></div><div><dt>Pay date</dt><dd>{current?.payDateLabel || '—'}</dd></div><div><dt>Period start</dt><dd>{current?.startDateLabel || '—'}</dd></div><div><dt>Period end</dt><dd>{current?.endDateLabel || '—'}</dd></div></dl></section>; }
PayrollRunStatusPanel.propTypes={model:PropTypes.object.isRequired};
