import PropTypes from 'prop-types';
export default function PayrollRunPeriodsTable({periods}) { if (!periods.length) return <section className="payroll-run-panel"><h2>Payroll periods</h2><p>No payroll periods were returned.</p></section>; return <section className="payroll-run-panel"><h2>Payroll periods</h2><div className="payroll-run-table-wrap"><table className="payroll-run-table"><thead><tr><th>Period</th><th>Frequency</th><th>Pay date</th><th>Status</th><th>Employees</th></tr></thead><tbody>{periods.map(period=><tr key={period.id}><td>{period.name}</td><td>{period.frequencyLabel}</td><td>{period.payDateLabel}</td><td>{period.statusLabel}</td><td>{period.employeeCountLabel}</td></tr>)}</tbody></table></div></section>; }
PayrollRunPeriodsTable.propTypes={periods:PropTypes.arrayOf(PropTypes.object).isRequired};
