export const DASHBOARD_EMPLOYEE_COUNT_PATHS = Object.freeze([
  'employeeCount',
  'employeesCount',
  'numberOfEmployees',
  'summary.employeeCount',
  'payroll.employeeCount',
]);

export const DASHBOARD_STATUS_PATHS = Object.freeze([
  'processingStatus',
  'status',
  'state',
  'payroll.status',
]);

export const DASHBOARD_HEALTH_PATHS = Object.freeze([
  'runtimeHealth',
  'health',
  'connectionStatus',
  'provider.status',
]);
