export const OTHER_IDENTITY_FIELDS = Object.freeze({
  payrollCode: Object.freeze([
    'payrollCode',
    'payrollId',
    'employeeCode',
  ]),

  paymentMethod: Object.freeze([
    'paymentMethod',
    'payMethod',
    'paymentType',
  ]),

  onHold: Object.freeze([
    'onHold',
    'isOnHold',
    'payrollOnHold',
  ]),

  excludeFromPayroll: Object.freeze([
    'excludeFromPayroll',
    'excludedFromPayroll',
    'doNotPay',
  ]),
});
